# A2A 生态与基础设施

A2A 如果成为 Agent 间通信标准，周边一定会出现一组基础设施。协议本身只是起点，真正的平台价值在发现、治理、安全、观测、计费和版本管理。

## Agent Registry

Agent Registry 是企业内部或生态平台中的 Agent 目录服务。

它负责：

- 收录可调用 Agent；
- 管理 Agent Card；
- 校验 Agent Card 签名；
- 标记可信来源；
- 管理版本和环境；
- 根据标签、领域、租户做检索；
- 控制哪些 Agent 可以被哪些调用方发现。

Registry 不是简单的文件列表，而是 Agent 生态中的服务发现和治理入口。

## Agent Gateway / Proxy

Agent Gateway 位于调用方和被调用 Agent 之间。

```text
Client Agent
    │
    ▼
Agent Gateway
    │
    ├── 鉴权
    ├── 限流
    ├── 审计
    ├── 策略
    ├── trace
    ├── 成本统计
    └── 安全过滤
    │
    ▼
Remote Agent
```

它适合承载：

- 认证鉴权；
- mTLS；
- Token Exchange；
- tenant 隔离；
- rate limit；
- quota；
- circuit breaker；
- prompt injection 检测；
- DLP；
- request / response logging；
- trace 注入；
- callback 校验；
- 成本归集。

如果没有 Gateway，每个 Agent 都要重复实现这些横切能力。

## Agent Identity Provider

Agent 需要自己的身份体系，而不只是用户身份。

需要回答：

- Agent 是否有唯一身份；
- Agent 身份如何签发；
- Agent 是否属于某个组织、项目、环境；
- Agent 的证书或 token 如何轮换；
- Agent 调用另一个 Agent 时，如何表达“我是 Agent A，代表用户 U”。

可能的实现：

- 企业 IAM；
- OAuth2 / OIDC；
- SPIFFE / SPIRE；
- mTLS 证书；
- workload identity；
- Agent 专用身份目录。

## Agent Observability

Agent Observability 不只是看日志。它需要覆盖：

- 请求链路；
- Task 生命周期；
- LLM 调用；
- 工具调用；
- MCP 调用；
- Artifact 生成；
- 回调；
- 成本；
- 安全拦截。

核心指标包括：

- Task 成功率；
- 平均耗时和 P95/P99；
- input-required 比例；
- failed / canceled 比例；
- 每个 Skill 的调用量；
- 每个 Agent 的成本；
- 下游依赖失败率；
- callback 失败率；
- 平均 token 消耗。

## Agent Marketplace

Agent Marketplace 类似应用商店或 npm，但分发的是 Agent 能力。

它可能提供：

- Agent 搜索；
- Agent Card 展示；
- 示例任务；
- 价格；
- SLA；
- 调用次数统计；
- 评分和认证；
- 使用协议；
- 计费结算；
- sandbox 测试。

企业内部也可以有私有 Marketplace，用于复用各团队建设的业务 Agent。

## Agent Schema Registry

Skill 的输入输出如果没有 schema 管理，很快会失控。

Schema Registry 负责：

- 管理 Skill 输入 schema；
- 管理 Artifact schema；
- 标记兼容性；
- 支持版本演进；
- 生成校验代码；
- 支持客户端按 schema 构造请求；
- 对 breaking change 做提醒。

这对企业落地尤其重要，因为自然语言描述不够稳定，真正的系统集成仍需要结构化契约。

## Agent Sandbox

在开放生态中，调用未知 Agent 或让未知 Agent 调用内部能力都很危险。

Sandbox 可以用于：

- 测试 Agent Card 是否真实；
- 运行示例任务；
- 限制网络；
- 限制成本；
- 限制工具权限；
- 观察行为；
- 检查输出是否合规。

## Agent Policy Engine

策略引擎回答“这次调用是否允许”。

策略输入可能包括：

- 调用方 Agent；
- 被调用 Agent；
- Skill；
- 用户；
- tenant；
- 数据分类；
- 成本预算；
- 地域；
- 时间；
- 风险等级；
- 历史行为。

策略输出可能是：

- allow；
- deny；
- require approval；
- allow with redaction；
- allow with lower budget；
- route to sandbox。

## 相关协议和生态

| 名称 | 定位 | 与 A2A 的关系 |
|---|---|---|
| MCP | Agent 连接工具和数据 | 互补 |
| ACP | Agent 间通信协议 | 类似定位，生态不同 |
| AGNTCY | Agent 网络和发现层 | 更偏网络和基础设施 |
| OpenAI Agents SDK | Agent 开发框架 | 可作为内部实现 |
| LangGraph / CrewAI / AutoGen / ADK | 多 Agent 编排框架 | 可包装为 A2A Agent |

## 建设优先级

如果从企业内部平台角度建设，建议优先级：

1. Agent Card 规范和内部 Registry；
2. A2A Gateway；
3. Task Store 和统一 trace；
4. Auth / OBO / tenant 权限；
5. Skill schema 管理；
6. 成本统计和配额；
7. Marketplace 和评分体系。

## 参考资料

- A2A 最新规范：<https://a2a-protocol.org/latest/specification/>
- Agent Discovery：<https://a2a-protocol.org/latest/topics/agent-discovery/>


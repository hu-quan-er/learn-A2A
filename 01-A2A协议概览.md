# A2A 协议概览

## A2A 解决的问题

2024 到 2025 年之后，Agent 系统快速增长，但行业出现了明显割裂：

- **框架割裂**：LangGraph、CrewAI、AutoGen、ADK、Bedrock Agents 等各自有自己的 Agent 抽象。
- **厂商割裂**：Salesforce、ServiceNow、SAP、Atlassian 等厂商都在建设 Agent，但默认互不认识。
- **能力孤岛**：不同 Agent 都能封装成 API，但调用方式、能力描述、状态语义、流式机制和回调方式并不统一。

企业真正关心的是：

- 采购 Agent 能不能直接调用供应商的报价 Agent？
- LangGraph 写的主管 Agent 能不能调度 CrewAI 写的执行 Agent？
- 企业内部不同团队写的 Agent 能不能像微服务一样被发现、调用、观测和治理？

A2A（Agent2Agent Protocol）的目标就是给这些问题提供一个共同协议层。

## A2A 的定位

A2A 是一个开放的 Agent 间通信协议，定位是跨框架、跨语言、跨厂商、跨组织的互操作标准。

它不要求调用方知道被调用 Agent 的内部实现，不要求共享模型、提示词、工具、记忆或状态。调用方只需要知道：

- 这个 Agent 在哪里；
- 它公开了哪些能力；
- 它支持哪些输入输出格式；
- 如何认证；
- 如何提交任务；
- 如何接收进度和结果。

官方最新规范已经发布 A2A v1.0，并将其描述为面向独立、可能不透明的 AI Agent 系统之间通信和互操作的开放标准。

## A2A 的基本交互

一个典型 A2A 调用可以分成四步：

```text
1. 发现 Agent Card
   Client 读取远端 Agent 的公开能力描述。

2. 选择 Skill
   Client 根据 Agent Card 中的 skills 判断远端 Agent 是否适合当前任务。

3. 提交 Message / Task
   Client 通过 A2A 协议接口发送请求，远端 Agent 开始处理。

4. 接收进度和结果
   对短任务直接返回 Message；对长任务返回 Task，并通过流式或回调更新状态。
```

## A2A 不解决什么

A2A 的边界也很重要：

- 它不规定 Agent 内部如何推理；
- 它不规定多 Agent 流程如何编排；
- 它不规定 Agent 使用哪个模型；
- 它不规定 Agent 连接哪些工具；
- 它不规定企业内部的权限系统如何实现；
- 它不直接解决 prompt injection、数据泄漏、成本分摊等生产治理问题。

这些问题需要由编排框架、MCP、企业 IAM、Agent Gateway、Observability 平台和业务系统共同解决。

## 可以类比成什么

A2A 在 Agent 时代的位置，接近微服务时代的 HTTP/gRPC + 服务契约 + 状态回调组合。

但它比普通 RPC 更关注 Agent 的特点：

- Agent 任务可能是长时间运行的；
- Agent 可能在执行过程中请求补充输入；
- 输入输出可能包含文本、文件、结构化数据等多种 Part；
- 一个调用可能产生多个产物；
- 调用方和被调用方可能来自不同组织。

## 这篇之后读什么

读完这篇后，建议继续读：

- [02-A2A核心模型.md](./02-A2A核心模型.md)
- [03-A2A通信机制与任务生命周期.md](./03-A2A通信机制与任务生命周期.md)
- [04-A2A与MCP的关系.md](./04-A2A与MCP的关系.md)

## 参考资料

- A2A 最新规范：<https://a2a-protocol.org/latest/specification/>
- A2A v1.0 发布说明：<https://a2a-protocol.org/latest/announcing-1.0/>


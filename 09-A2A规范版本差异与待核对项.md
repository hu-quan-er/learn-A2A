# A2A 规范版本差异与待核对项

A2A 仍在快速演进。学习和写代码时，必须明确自己参考的是哪个规范版本、哪个 SDK 版本。否则很容易遇到字段名、路径、方法名不一致的问题。

## 当前应优先关注的版本

官方 latest 规范已指向 A2A v1.0.0。后续新学习和新实现应优先以 latest / v1.0.0 为准，同时在阅读旧文章和旧样例时注意差异。

## 发现路径变化

旧资料中常见：

```text
/.well-known/agent.json
```

新规范和 latest 教程中更应关注：

```text
/.well-known/agent-card.json
```

官方 roadmap 曾明确提到根据 IANA 反馈将 Agent Card 路径从 `agent.json` 调整为 `agent-card.json`。因此新文档、新实现和新样例应优先使用 `agent-card.json`。

## 协议方法命名变化

早期资料中常见方法：

```text
tasks/send
tasks/sendSubscribe
tasks/pushNotification/set
tasks/pushNotification/get
```

latest 规范和较新的 SDK 中更常看到：

```text
message/send
message/stream
tasks/get
tasks/cancel
tasks/resubscribe
tasks/pushNotificationConfig/set
tasks/pushNotificationConfig/get
```

需要注意：不同协议绑定、不同 SDK 可能有命名差异。写代码前必须锁定目标 SDK 版本。

## 上下文字段变化

旧资料中可能用：

```text
sessionId
```

latest 规范更应关注：

```text
contextId
```

学习时可以把它们都理解为“关联多轮交互或多个任务的上下文标识”，但代码和文档应使用当前目标规范中的字段名。

## Agent Card 结构变化

早期资料常把 Agent Card 简化为：

```json
{
  "name": "Agent",
  "url": "https://example.com/a2a",
  "capabilities": {},
  "authentication": {},
  "skills": []
}
```

latest 规范更强调：

- `protocolVersion`；
- `supportedInterfaces`；
- `securitySchemes`；
- `security`；
- `capabilities`；
- `skills`；
- Agent Card 签名；
- 多协议绑定。

因此“url + skills + auth”的入门模型仍然有用，但不足以覆盖生产实现。

## 协议绑定变化

旧理解：

```text
A2A = JSON-RPC over HTTPS + SSE + Webhook
```

更准确的新理解：

```text
A2A 定义共同语义模型，并可以通过 JSON-RPC、gRPC、HTTP+JSON 等协议绑定表达。
```

这意味着新架构文档应将“语义模型”和“传输/绑定”分开描述。

## 安全对象变化

latest 规范中的安全模型更接近 OpenAPI 风格，包含：

- API Key；
- HTTP Auth；
- OAuth2；
- OpenID Connect；
- mTLS。

并且 Agent Card 不应包含明文敏感凭据。

后续需要进一步核对：

- securitySchemes 的准确字段结构；
- skill 级别 security requirements 如何表达；
- Agent Card 签名如何生成和校验；
- authenticated extended card 如何使用。

## 需要持续核对的清单

每次升级 A2A SDK 或参考新规范时，检查：

- latest 版本号；
- Agent Card well-known 路径；
- Agent Card 字段名；
- supportedInterfaces 是否必需；
- protocol binding 支持情况；
- message / task 方法名；
- Task 状态枚举；
- context id 字段；
- Part 字段名；
- push notification 方法和数据结构；
- error code 定义；
- security scheme 定义；
- SDK 示例是否和规范一致。

## 本组文档的处理原则

这组文档保留入门友好的概念解释，但在涉及实现细节时优先采用 latest / v1.0.0 方向：

- 使用 `agent-card.json` 作为默认发现路径；
- 使用 `contextId` 作为上下文标识；
- 用 `message/send`、`message/stream` 描述较新的消息发送模型；
- 将 JSON-RPC、gRPC、HTTP+JSON 视为协议绑定；
- 在生产章节中加入 Agent Card 签名、安全方案、多协议绑定、版本协商等主题。

如果后续要写代码实现，应以具体 SDK 的类型定义为准，而不是只看本组学习文档。

## 参考资料

- A2A latest 规范：<https://a2a-protocol.org/latest/specification/>
- A2A v0.3.0 规范：<https://a2a-protocol.org/v0.3.0/specification/>
- A2A v0.2.0 规范：<https://a2a-protocol.org/v0.2.0/specification/>
- A2A roadmap：<https://a2a-protocol.org/v0.3.0/roadmap/>
- A2A v1.0 发布说明：<https://a2a-protocol.org/latest/announcing-1.0/>


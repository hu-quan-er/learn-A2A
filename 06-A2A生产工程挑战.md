# A2A 生产工程挑战

A2A 规范解决的是 Agent 间通信的共同语言，但生产系统还要解决信任、身份、成本、观测、版本、状态、限流、错误、安全等问题。这些才是真正落地时的难点。

## 信任与身份

核心问题：

- 我怎么确认对方真的是它声称的 Agent？
- Agent Card 有没有被篡改？
- Agent A 代表用户调用 Agent B 时，用户身份如何传递？
- 跨组织调用时，谁能代表谁？

可研究方向：

- Agent Card 签名；
- mTLS；
- OAuth2 Client Credentials；
- OAuth2 Token Exchange；
- OBO（on-behalf-of）流程；
- 企业 Agent Registry；
- Agent 身份证书生命周期管理。

最新规范已经对 Agent Card 签名、安全方案声明、well-known 发现安全性有更明确描述，值得单独细读。

## 成本归属

多 Agent 调用链会带来成本归属问题：

```text
用户请求
  └── Agent A
        └── Agent B
              └── Agent C
                    └── LLM / 工具 / 检索 / 文件处理
```

需要回答：

- token 费用算谁的；
- 外部工具费用算谁的；
- 失败重试的成本算谁的；
- 预算超限时由谁中断；
- 账单粒度是用户、租户、Agent、Task 还是 Skill。

建议在 metadata 或 header 中传播：

- tenant id；
- user id 或 subject；
- cost center；
- budget id；
- request id；
- trace id；
- max cost / max tokens / max duration。

这些字段目前更偏工程约定，尚未形成完全统一的事实标准。

## 跨 Agent 可观测性

生产问题一定会跨多个 Agent：

- 哪个 Agent 慢；
- 哪个 Agent 返回了错误；
- 哪个工具调用导致失败；
- 哪个任务重试了；
- 哪个 Artifact 丢失或生成失败；
- 哪个 Agent 消耗了最多 token。

建议设计三层观测：

1. **协议层**：A2A 请求、响应、状态更新、回调。
2. **任务层**：Task 状态、耗时、失败原因、Artifact。
3. **内部执行层**：LLM 调用、MCP 工具调用、数据库/API 调用。

关键技术：

- W3C Trace Context；
- OpenTelemetry；
- GenAI semantic conventions；
- A2A Task metadata；
- 统一 request id / task id / context id。

## 版本演进

A2A 仍在演进，版本问题不可忽略。

需要管理：

- 协议版本；
- SDK 版本；
- Agent Card schema 版本；
- Skill 输入输出 schema 版本；
- Artifact 格式版本；
- 业务能力版本；
- 客户端兼容策略。

建议：

- Agent Card 明确声明协议版本和接口；
- Skill id 保持稳定；
- breaking change 新增 skill id 或新版本 schema；
- 客户端按 capability 和 version 做降级；
- 将 schema 注册到 Schema Registry；
- 在灰度期间同时保留旧接口和新接口。

## 任务状态持久化

长任务不能只存在内存里。生产系统至少要持久化：

- task id；
- context id；
- 当前状态；
- 状态更新时间；
- message history；
- artifact metadata；
- callback config；
- trace id；
- error；
- retry count。

常见存储组合：

- Redis：短期状态、锁、进度；
- PostgreSQL / MySQL：任务主记录和审计；
- Object Storage：大文件 Artifact；
- Queue：异步执行和回调重试；
- Vector Store：可选，用于长期语义检索。

## 限流、配额与 SLA

Agent 对 Agent 的调用比普通 API 更容易失控，因为一个请求可能展开成多个子调用。

需要控制：

- 每个客户端并发数；
- 每个 tenant 的 QPS；
- 每个 Task 的最大执行时间；
- 最大 Agent 调用深度；
- 最大 fan-out；
- 最大 token / 成本；
- 每个 Skill 的 SLA；
- 回调重试次数。

建议在 Agent Gateway 层统一做：

- rate limit；
- quota；
- circuit breaker；
- timeout；
- retry policy；
- priority queue；
- admission control。

## 错误语义

JSON-RPC 或 HTTP 错误码只能表达协议层问题，业务错误需要额外标准化。

建议错误对象至少包含：

```json
{
  "code": "INVOICE_ORDER_NOT_FOUND",
  "message": "订单不存在，无法开具发票",
  "retryable": false,
  "category": "business",
  "taskId": "task_123",
  "traceId": "trace_456"
}
```

错误分类建议：

- `protocol`：协议或格式错误；
- `auth`：认证鉴权失败；
- `quota`：限流或配额；
- `timeout`：超时；
- `dependency`：下游依赖失败；
- `business`：业务规则失败；
- `safety`：安全策略拒绝；
- `unknown`：未分类错误。

## 编排边界

A2A 只定义两两通信，不负责整体编排。

多 Agent 协作通常有两种模式：

- **Orchestration**：中心编排器决定调用顺序和依赖关系；
- **Choreography**：多个 Agent 通过事件或任务互相协作，没有单一中心。

生产中更推荐关键流程使用 Orchestration，因为它更容易治理、观测、回滚和限流。

## 安全与对抗

A2A 引入了新的安全面：

- 恶意 Agent 伪造能力；
- Agent Card 被篡改；
- prompt injection 通过 A2A 输入传入；
- 文件 Part 携带恶意内容；
- Agent A 泄漏敏感数据给 Agent B；
- Agent 互调形成循环；
- 外部 Agent 诱导内部 Agent 调用高权限工具。

建议措施：

- Agent Card 签名和可信 Registry；
- Gateway 策略校验；
- 输入 schema 校验；
- 文件扫描；
- prompt injection 检测；
- 最小权限；
- 调用图最大深度限制；
- 敏感数据脱敏；
- 审计日志；
- 高风险操作人工审批。

## 与 MCP 混用时的挑战

当 A2A Agent 内部使用 MCP 工具时，风险会进一步扩展：

- A2A 输入能否直接传入 MCP 工具；
- MCP 工具权限是否按最终用户收敛；
- 工具调用结果是否可以回传给外部 Agent；
- 是否记录跨 A2A 和 MCP 的完整链路；
- prompt injection 是否可能影响工具调用参数。

这部分需要结合 [04-A2A与MCP的关系.md](./04-A2A与MCP的关系.md) 一起学习。

## 参考资料

- A2A 最新规范：<https://a2a-protocol.org/latest/specification/>
- A2A Agent Discovery：<https://a2a-protocol.org/latest/topics/agent-discovery/>


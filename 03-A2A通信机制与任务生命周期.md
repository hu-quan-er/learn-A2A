# A2A 通信机制与任务生命周期

A2A 的通信机制围绕一个核心问题展开：Agent 任务往往不是一次短平快的 API 调用，而是可能需要流式输出、补充输入、异步回调和状态恢复的长任务。

## 协议绑定

A2A 最新规范强调协议绑定的概念。同一个 Agent 可以通过不同传输和协议形式暴露能力，例如：

- JSON-RPC；
- gRPC；
- HTTP+JSON。

早期资料常把 A2A 简化为“JSON-RPC over HTTPS + SSE + Webhook”。这个理解对入门有帮助，但最新规范已经把协议绑定抽象得更清晰。学习时应区分：

- **语义层**：Message、Task、Artifact、状态流转；
- **协议绑定层**：这些语义通过 JSON-RPC、gRPC 或 HTTP+JSON 如何表达。

## 典型方法

在 JSON-RPC 绑定中，常见方法包括：

| 方法 | 作用 | 适用场景 |
|---|---|---|
| `message/send` | 发送消息或创建任务 | 普通调用 |
| `message/stream` | 发送消息并订阅流式结果 | 需要实时进度 |
| `tasks/get` | 查询任务状态 | 轮询、恢复、补偿 |
| `tasks/cancel` | 取消任务 | 用户撤销、超时控制 |
| `tasks/resubscribe` | 重新订阅任务流 | SSE 断线恢复 |
| `tasks/pushNotificationConfig/set` | 设置任务回调 | 长任务异步通知 |
| `tasks/pushNotificationConfig/get` | 获取回调配置 | 调试、审计 |

早期资料中可能看到 `tasks/send`、`tasks/sendSubscribe`、`tasks/pushNotification/set` 等命名。新文档应以目标规范版本和 SDK 为准。

## Message 与 Task 的关系

A2A 可以处理两类交互：

1. **无状态消息交互**  
   适合一次性、短任务、无需后续跟踪的场景。

2. **有状态任务交互**  
   适合长任务、多轮交互、需要进度、需要取消、需要恢复、需要最终产物的场景。

可以简单理解：

```text
短任务：Message -> Message
长任务：Message -> Task -> 状态更新 -> Artifact
```

## Task 生命周期

一个典型 Task 状态流转如下：

```text
submitted
   │
   ▼
working ───────────► completed
   │
   ├───────────────► failed
   │
   ├───────────────► canceled
   │
   └──► input-required
             │
             ▼
          working
```

关键状态解释：

- `submitted`：任务已提交，但还没真正开始处理；
- `working`：任务正在执行；
- `input-required`：远端 Agent 需要调用方补充输入；
- `completed`：任务完成，并可能带有 artifact；
- `failed`：任务失败；
- `canceled`：任务被取消。

## 一次流式交互

```text
Client                                  A2A Server
  │                                         │
  │── message/stream ─────────────────────►│
  │                                         │
  │◄── Task created / working ─────────────│
  │◄── status update: working ─────────────│
  │◄── status update: input-required ──────│
  │                                         │
  │── message/send with same context ─────►│
  │                                         │
  │◄── status update: working ─────────────│
  │◄── artifact + completed ───────────────│
```

重点不是方法名本身，而是这三个语义：

- 同一个上下文可以继续交互；
- 远端 Agent 可以主动要求补充输入；
- 最终结果可以作为 Artifact 返回。

## 流式与异步通知

A2A 中有两种常见进度获取方式：

| 机制 | 特点 | 适用场景 |
|---|---|---|
| SSE / Stream | 客户端保持连接，实时接收事件 | 交互式任务、前端展示进度 |
| Push Notification | 服务端回调客户端 URL | 长任务、后台任务、跨系统异步集成 |

两者不是互斥关系。一个系统可以在前台使用流式，在后台同时设置 Push Notification 作为可靠通知机制。

## 断线恢复

长任务一定要考虑连接中断。至少需要三层机制：

1. **Task Store**  
   服务端持久化任务状态、历史、产物和状态更新时间。

2. **重新订阅**  
   客户端断线后通过任务 id 重新订阅或查询状态。

3. **幂等补偿**  
   客户端收到重复回调或重复状态更新时，不应重复执行业务副作用。

## 错误处理

协议层错误和业务层错误要区分：

- 协议层错误：请求格式错误、方法不存在、鉴权失败、版本不兼容；
- 任务层错误：任务执行失败、用户输入不足、远端依赖超时；
- 业务层错误：订单不存在、余额不足、发票已作废。

实践中建议在错误数据中至少包含：

- 稳定错误码；
- 可读错误消息；
- 是否可重试；
- 是否需要人工介入；
- 原始业务上下文 id；
- trace id。

## 生产实现要点

实现 A2A Server 时，通信层之外还要设计：

- Task id 和 context id 生成规则；
- Task Store 存储结构；
- SSE 事件重放策略；
- Push Notification 重试和签名；
- 取消任务的实际执行语义；
- Artifact 的存储位置和访问权限；
- 任务超时和过期清理。

## 参考资料

- A2A 最新规范：<https://a2a-protocol.org/latest/specification/>
- Task 生命周期专题：<https://a2a-protocol.org/latest/topics/life-of-a-task/>


# A2A 核心模型

A2A 的核心不是某个 SDK，而是一组稳定的数据对象和交互语义。理解这些对象，就能理解 A2A 协议的大部分设计。

## 核心对象总览

| 对象 | 作用 | 重点 |
|---|---|---|
| Agent Card | Agent 的公开能力说明 | 发现、协议绑定、认证、skills |
| Agent Skill | Agent 能力单元 | 能力路由、输入输出模式、安全要求 |
| Message | 一次交互消息 | role + parts |
| Part | 消息或产物的内容片段 | text、file、data |
| Task | 有状态任务 | 生命周期、历史、产物、状态更新 |
| Artifact | 任务产物 | 一个任务可产生多个最终交付物 |
| Push Notification Config | 异步通知配置 | 长任务状态变更回调 |

## Agent Card

Agent Card 是 A2A 的发现入口，类似 Agent 的数字名片。它描述这个 Agent 是谁、能做什么、在哪里调用、支持哪些协议、需要什么认证。

在最新规范中，Agent Card 通常通过下面路径暴露：

```text
https://{server_domain}/.well-known/agent-card.json
```

早期规范和一些旧资料中也会看到：

```text
https://{server_domain}/.well-known/agent.json
```

后续学习和新实现应优先以最新规范的 `agent-card.json` 为准。

一个简化后的 Agent Card 可以理解为：

```json
{
  "name": "Invoice Agent",
  "description": "处理发票开具、查询、作废",
  "version": "1.2.0",
  "capabilities": {
    "streaming": true,
    "pushNotifications": true
  },
  "defaultInputModes": ["text/plain", "application/json"],
  "defaultOutputModes": ["text/plain", "application/pdf"],
  "skills": [
    {
      "id": "issue_invoice",
      "name": "开具发票",
      "description": "根据订单号生成发票 PDF",
      "tags": ["billing", "invoice"],
      "examples": ["给订单 1234 开发票"],
      "inputModes": ["application/json"],
      "outputModes": ["application/pdf"]
    }
  ]
}
```

最新规范还引入和强调了 `supportedInterfaces`，用于声明同一个 Agent 可通过哪些协议绑定和 URL 访问，例如 JSON-RPC、gRPC、HTTP+JSON 等。

## Agent Skill

Skill 是 Agent Card 中最适合做能力路由的部分。它回答：

- 这个 Agent 具体能完成什么任务；
- 这个能力适合哪些输入；
- 会产出什么格式；
- 需要什么权限；
- 有哪些示例提示词或调用场景。

设计 Skill 时不要只写笼统描述，例如“处理财务问题”。更好的方式是拆成：

- `issue_invoice`：开具发票；
- `query_invoice`：查询发票；
- `void_invoice`：作废发票；
- `export_invoice_pdf`：导出发票 PDF。

这样调用方或编排器才能做更可靠的能力匹配。

## Message

Message 表示一次交互消息，通常包含：

- `role`：消息角色，例如 user 或 agent；
- `parts`：消息内容片段数组。

示例：

```json
{
  "role": "user",
  "parts": [
    { "kind": "text", "text": "请给这张订单开发票" },
    { "kind": "data", "data": { "orderId": "1234", "amount": 999 } }
  ]
}
```

不同规范版本和 SDK 中，字段名可能存在 `type` / `kind`、驼峰 / 下划线等差异。写文档和代码时要以目标 SDK 对应版本为准。

## Part

Part 是 A2A 支持多模态和结构化交互的关键。

常见 Part 类型：

- **Text Part**：纯文本；
- **File Part**：文件，可通过 bytes 或 URI 表示；
- **Data Part**：结构化 JSON 数据。

Part 让 A2A 可以表达“文本 + PDF + JSON 参数 + 图片”这类混合输入输出，而不只是传统 API 的单一 JSON body。

## Task

Task 是 A2A 的有状态工作单元。它适合表示需要持续跟踪、可能多轮交互、可能长时间运行的任务。

一个 Task 通常包含：

- `id`：任务标识；
- `contextId`：上下文标识，用来关联同一上下文中的多次交互；
- `status`：当前状态；
- `history`：消息历史；
- `artifacts`：任务产物；
- `metadata`：扩展信息，例如 trace id、业务 id、成本上下文。

早期资料中可能使用 `sessionId` 描述关联上下文；最新规范更应关注 `contextId`。

## Artifact

Artifact 是任务产物，和 Message 一样可以由多个 Part 组成，但语义不同：

- Message 是交互过程中的消息；
- Artifact 是任务最终或阶段性的交付物。

例如“开具发票”任务可能产出：

- PDF 发票文件；
- 发票结构化 JSON；
- 审计日志摘要；
- 付款链接。

## Push Notification Config

长任务不能要求调用方一直保持连接。A2A 提供 Push Notification 配置，让服务端在任务状态变化时主动通知客户端。

简化理解：

```json
{
  "url": "https://client.example.com/a2a/callbacks",
  "token": "opaque-correlation-token",
  "authentication": {
    "schemes": ["Bearer"]
  }
}
```

生产系统要额外考虑：

- 回调地址校验；
- 回调重试；
- 幂等处理；
- 签名或认证；
- 回调失败后的补偿查询。

## 深入学习重点

继续学习核心模型时，建议重点看：

- Agent Card 字段级定义；
- Skill 与输入输出 schema 的关系；
- Task 与 Message 的边界；
- Artifact 是否作为最终产物还是阶段性产物；
- metadata 如何承载 trace、cost、tenant、业务 id；
- 不同版本规范中的字段差异。

## 参考资料

- A2A 最新规范：<https://a2a-protocol.org/latest/specification/>
- Agent Skills 与 Agent Card 教程：<https://a2a-protocol.org/latest/tutorials/python/3-agent-skills-and-card/>


# A2A 学习路径与实践路线

学习 A2A 不建议一开始就陷入 SDK 细节。更好的路线是先理解协议模型，再跑最小样例，最后进入生产工程问题。

## 第一阶段：理解协议边界

目标：能清楚说出 A2A、MCP、多 Agent 框架分别解决什么问题。

要读：

- [01-A2A协议概览.md](./01-A2A协议概览.md)
- [04-A2A与MCP的关系.md](./04-A2A与MCP的关系.md)
- [05-多Agent调度框架对比.md](./05-多Agent调度框架对比.md)

需要掌握：

- A2A 是 Agent 对 Agent；
- MCP 是 Agent 对工具和数据；
- LangGraph / CrewAI / AutoGen / ADK 是内部编排；
- A2A 不替代编排框架，也不替代 MCP。

## 第二阶段：读规范核心对象

目标：能读懂 Agent Card、Skill、Message、Task、Artifact。

要读：

- [02-A2A核心模型.md](./02-A2A核心模型.md)
- [03-A2A通信机制与任务生命周期.md](./03-A2A通信机制与任务生命周期.md)
- A2A 官方最新规范

重点问题：

- Agent Card 中哪些字段用于发现；
- Skill 如何表达能力；
- Message 和 Task 的区别；
- Part 如何表达文本、文件、结构化数据；
- Artifact 和 Message 的语义差异；
- Task 状态如何流转；
- context id 如何关联多轮任务。

## 第三阶段：跑官方样例

目标：让一个最小 A2A Agent 真正跑起来。

建议顺序：

1. 克隆官方样例仓库；
2. 跑 Python hello world Agent；
3. 查看 `.well-known/agent-card.json`；
4. 用客户端读取 Agent Card；
5. 发送一个最小 message；
6. 观察返回 Message 或 Task。

记录这些问题：

- Agent Card 实际长什么样；
- SDK 字段名和规范字段名是否一致；
- 默认使用哪个协议绑定；
- 是否支持 streaming；
- 日志里如何记录 task id / context id。

## 第四阶段：写最小客户端和服务端

目标：不依赖复杂框架，手写一个能理解 A2A 基本语义的小系统。

建议实现一个“发票 Agent”：

- Skill：`issue_invoice`；
- 输入：订单 id、抬头、税号；
- 输出：PDF URI + 结构化 JSON；
- 支持：普通调用、流式状态、任务查询。

最小能力：

- 暴露 Agent Card；
- 接收 Message；
- 创建 Task；
- 更新 Task 状态；
- 返回 Artifact；
- 支持 `tasks/get`。

进阶能力：

- 支持 stream；
- 支持 cancel；
- 支持 Push Notification；
- 支持 Task Store；
- 支持 trace id。

## 第五阶段：接入一个编排框架

目标：把内部多 Agent 工作流包装成 A2A Agent。

推荐顺序：

1. LangGraph：适合理解状态机和持久化；
2. CrewAI：适合理解角色任务式协作；
3. ADK：适合理解 A2A 原生支持；
4. AutoGen：适合理解对话式多 Agent 协作。

包装时关注：

- A2A Message 如何转成内部输入；
- 内部状态如何映射到 A2A Task；
- 内部流式输出如何映射到 A2A event；
- 内部最终结果如何映射到 Artifact；
- 内部错误如何映射到标准错误。

## 第六阶段：补生产能力

目标：从 Demo 走向可治理系统。

优先补：

1. Task Store；
2. Agent Card 版本管理；
3. 认证和鉴权；
4. trace id 贯穿；
5. 错误分类；
6. 限流和超时；
7. callback 重试；
8. Artifact 存储和权限；
9. 成本统计；
10. 安全过滤。

这时重点阅读：

- [06-A2A生产工程挑战.md](./06-A2A生产工程挑战.md)
- [07-A2A生态与基础设施.md](./07-A2A生态与基础设施.md)

## 第七阶段：做一个完整案例

建议案例：跨组织采购协作。

```text
采购 Agent
   ├── A2A 调报价 Agent
   ├── A2A 调物流 Agent
   ├── A2A 调开票 Agent
   └── A2A 调付款 Agent
```

每个 Agent 内部可以用不同实现：

- 报价 Agent：CrewAI；
- 物流 Agent：普通服务 + MCP；
- 开票 Agent：LangGraph；
- 付款 Agent：强规则服务；
- 主控 Agent：LangGraph 或 ADK。

这个案例能覆盖：

- discovery；
- skill routing；
- auth；
- task lifecycle；
- streaming；
- push notification；
- artifact；
- trace；
- cost；
- error handling。

## 学习产出建议

每阶段都输出一个小文档：

- A2A / MCP / 框架边界图；
- Agent Card 字段表；
- Task 生命周期图；
- 最小 Demo 运行记录；
- 框架接入对照表；
- 生产能力 checklist；
- 完整案例架构图。

## 参考资料

- A2A 最新规范：<https://a2a-protocol.org/latest/specification/>
- A2A GitHub：<https://github.com/a2aproject>
- A2A Python SDK：<https://github.com/a2aproject/a2a-python>
- A2A Samples：<https://github.com/a2aproject/a2a-samples>
- MCP 官方文档：<https://modelcontextprotocol.io/>

